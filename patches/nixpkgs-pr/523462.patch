From 81eaa657eb00715bd2e671e0c2378296b59e14d5 Mon Sep 17 00:00:00 2001
From: ccicnce113424 <ccicnce113424@gmail.com>
Date: Sun, 24 May 2026 00:52:10 +0800
Subject: [PATCH 1/3] scx-loader: init at 1.1.1

---
 pkgs/by-name/sc/scx-loader/package.nix | 53 ++++++++++++++++++++++++++
 1 file changed, 53 insertions(+)
 create mode 100644 pkgs/by-name/sc/scx-loader/package.nix

diff --git a/pkgs/by-name/sc/scx-loader/package.nix b/pkgs/by-name/sc/scx-loader/package.nix
new file mode 100644
index 0000000000000..10d76a2765df6
--- /dev/null
+++ b/pkgs/by-name/sc/scx-loader/package.nix
@@ -0,0 +1,53 @@
+{
+  fetchFromGitHub,
+  rustPlatform,
+  lib,
+  nix-update-script,
+}:
+rustPlatform.buildRustPackage (finalAttrs: {
+  pname = "scx-loader";
+  version = "1.1.1";
+
+  cargoHash = "sha256-uX2lCVDa8eAKWi/bj94+JQHoOLll0OjKRHT0EPZELNc=";
+
+  src = fetchFromGitHub {
+    owner = "sched-ext";
+    repo = "scx-loader";
+    tag = "v${finalAttrs.version}";
+    hash = "sha256-5OvdtW/Li+ubHDBSKe2ssE9ZyNSCcxNFSJffzxQ9WMk=";
+  };
+
+  __structuredAttrs = true;
+
+  env = {
+    VENDOR_PREFIX = "";
+    VENDOR_DATADIR = "/share";
+  };
+
+  postInstall = ''
+    cargo xtask install --destdir $out
+    rm $out/bin/xtask
+  '';
+
+  postFixup = ''
+    substituteInPlace $out/lib/systemd/system/scx_loader.service \
+      --replace-fail "/usr/bin/scx_loader" "$out/bin/scx_loader"
+    substituteInPlace $out/share/dbus-1/system-services/org.scx.Loader.service \
+      --replace-fail "/usr/bin/scx_loader" "$out/bin/scx_loader"
+  '';
+
+  passthru.updateScript = nix-update-script { };
+
+  meta = {
+    mainProgram = "scxctl";
+    homepage = "https://github.com/sched-ext/scx-loader";
+    changelog = "https://github.com/sched-ext/scx-loader/releases/tag/v${finalAttrs.version}";
+    license = lib.licenses.gpl2Only;
+    platforms = lib.platforms.linux;
+    maintainers = with lib.maintainers; [
+      Gliczy
+      michaelBelsanti
+      ccicnce113424
+    ];
+  };
+})

From f1aef7561dbd5ee8cab073c78c350066fb61d99c Mon Sep 17 00:00:00 2001
From: ccicnce113424 <ccicnce113424@gmail.com>
Date: Sun, 24 May 2026 00:52:10 +0800
Subject: [PATCH 2/3] nixos/scx-loader: init

---
 .../manual/release-notes/rl-2611.section.md   |   2 +-
 nixos/modules/module-list.nix                 |   1 +
 .../services/scheduling/scx-loader.nix        | 139 ++++++++++++++++++
 3 files changed, 141 insertions(+), 1 deletion(-)
 create mode 100644 nixos/modules/services/scheduling/scx-loader.nix

diff --git a/nixos/doc/manual/release-notes/rl-2611.section.md b/nixos/doc/manual/release-notes/rl-2611.section.md
index 3d53f1f209307..1e6de73837f9a 100644
--- a/nixos/doc/manual/release-notes/rl-2611.section.md
+++ b/nixos/doc/manual/release-notes/rl-2611.section.md
@@ -10,7 +10,7 @@
 
 <!-- To avoid merge conflicts, consider adding your item at an arbitrary place in the list instead. -->
 
-- Create the first release note entry in this section!
+- [scx_loader](https://github.com/sched-ext/scx-loader), a system daemon and DBus-based loader for sched_ext schedulers. `scxctl` is the command-line client for interacting with the loader, allowing users to switch schedulers, modes, and arguments dynamically. Available as [services.scx-loader](#opt-services.scx-loader.enable)
 
 ## Backward Incompatibilities {#sec-release-26.11-incompatibilities}
 
diff --git a/nixos/modules/module-list.nix b/nixos/modules/module-list.nix
index c57b627e875c7..e977edb5782a6 100644
--- a/nixos/modules/module-list.nix
+++ b/nixos/modules/module-list.nix
@@ -1482,6 +1482,7 @@
   ./services/scheduling/cron.nix
   ./services/scheduling/fcron.nix
   ./services/scheduling/prefect.nix
+  ./services/scheduling/scx-loader.nix
   ./services/scheduling/scx.nix
   ./services/search/elasticsearch-curator.nix
   ./services/search/elasticsearch.nix
diff --git a/nixos/modules/services/scheduling/scx-loader.nix b/nixos/modules/services/scheduling/scx-loader.nix
new file mode 100644
index 0000000000000..7889b0535b3b2
--- /dev/null
+++ b/nixos/modules/services/scheduling/scx-loader.nix
@@ -0,0 +1,139 @@
+{
+  lib,
+  pkgs,
+  config,
+  ...
+}:
+let
+  cfg = config.services.scx-loader;
+  modes = [
+    "Auto"
+    "Gaming"
+    "LowLatency"
+    "PowerSave"
+    "Server"
+  ];
+
+  schedulers = lib.concatMap (
+    scx: scx.passthru.schedulers or (throw "Scheduler not found in package ${scx.name}")
+  ) cfg.schedsPackages;
+
+  # scx_loader does not recognize table names enclosed in quotation marks; therefore, we need to manually generate the configuration file content here.
+  configFile =
+    let
+      tableLine = sched: "[scheds.${sched}]";
+      flagLine = mode: flags: "${lib.toLower mode}_mode=${lib.toJSON flags}";
+      schedTable =
+        sched: flags: lib.concatLines ([ (tableLine sched) ] ++ lib.mapAttrsToList flagLine flags);
+    in
+    pkgs.writeText "scx_loader.toml" ''
+      default_sched="${cfg.defaultSched}"
+      default_mode="${cfg.defaultMode}"
+      ${lib.concatMapAttrsStringSep "\n" schedTable cfg.schedsCfg}
+      ${lib.optionalString (cfg.extraConfig != null) cfg.extraConfig}
+    '';
+in
+{
+  options.services.scx-loader = {
+    enable = lib.mkEnableOption ''
+      SCX Loader service, a daemon to run schedulers from userspace using dbus. This requires kernel version 6.12 and later.
+    '';
+
+    package = lib.mkPackageOption pkgs "scx-loader" { };
+
+    schedsPackages = lib.mkOption {
+      type = lib.types.listOf lib.types.package;
+      default = [ pkgs.scx.rustscheds ];
+      defaultText = lib.literalExpression "[ pkgs.scx.rustscheds ]";
+      example = lib.literalExpression "[ pkgs.scx.full ]";
+      description = ''
+        `scx` package to use. `scx.rustscheds`, which includes all schedulers currently supported by `scx_loader`, is the default.
+      '';
+    };
+
+    defaultSched = lib.mkOption {
+      type = lib.types.str;
+      default = "scx_bpfland";
+      example = "scx_lavd";
+      description = ''
+        Default scheduler that will be started automatically when `scx_loader` starts.
+        If set to an empty string, `scx_loader` will not start any scheduler by default.
+      '';
+    };
+
+    defaultMode = lib.mkOption {
+      type = lib.types.enum modes;
+      default = "Auto";
+      description = ''
+        Default scheduler mode that will be used when starting a scheduler.
+      '';
+    };
+
+    schedsCfg = lib.mkOption {
+      type = lib.types.attrsOf (
+        lib.types.submodule {
+          options = lib.genAttrs (map lib.toCamelCase modes) (
+            mode:
+            lib.mkOption {
+              type = lib.types.listOf lib.types.str;
+              default = [ ];
+              description = ''
+                Specify the flags to be used for schedulers in "${lib.toSentenceCase mode}" mode.
+                If an empty list is provided, the default flags for that mode will be used.
+              '';
+            }
+          );
+        }
+      );
+      description = ''
+        The custom flags for a specific scheduler.
+      '';
+    };
+
+    extraConfig = lib.mkOption {
+      type = lib.types.nullOr lib.types.lines;
+      default = null;
+      description = ''
+        Extra configuration content to be appended to the generated configuration file.
+      '';
+    };
+  };
+
+  config = lib.mkIf cfg.enable {
+    assertions = [
+      {
+        assertion = lib.versionAtLeast config.boot.kernelPackages.kernel.version "6.12";
+        message = "SCX is only supported on kernel version >= 6.12.";
+      }
+      {
+        assertion = !config.services.scx.enable;
+        message = "services.scx and services.scx_loader cannot be enabled simultaneously. Please enable only one of them.";
+      }
+      {
+        assertion = lib.elem cfg.defaultSched ([ "" ] ++ schedulers);
+        message = "The default scheduler must be provided by the selected scx package.";
+      }
+      {
+        assertion = lib.all (sched: lib.elem sched schedulers) (lib.attrNames cfg.schedsCfg);
+        message = "All schedulers in the configuration must be provided by the selected scx package.";
+      }
+    ];
+
+    environment.systemPackages = [ cfg.package ] ++ cfg.schedsPackages;
+    systemd.packages = [ cfg.package ];
+    services.dbus.packages = [ cfg.package ];
+
+    security.polkit.enable = true;
+
+    systemd.services.scx_loader = {
+      path = cfg.schedsPackages;
+      wantedBy = [ "multi-user.target" ];
+      serviceConfig = {
+        TemporaryFileSystem = [ "/etc" ];
+        BindReadOnlyPaths = [ "${configFile.outPath}:/etc/scx_loader.toml" ];
+      };
+    };
+
+    meta.maintainers = with lib.maintainers; [ ccicnce113424 ];
+  };
+}

From 1263a2aca0dc9e4e9928c4b70f944b9176f2b983 Mon Sep 17 00:00:00 2001
From: ccicnce113424 <ccicnce113424@gmail.com>
Date: Sun, 24 May 2026 00:52:10 +0800
Subject: [PATCH 3/3] nixosTests.scx-loader: init

---
 nixos/tests/all-tests.nix              |  1 +
 nixos/tests/scx/loader.nix             | 20 ++++++++++++++++++++
 pkgs/by-name/sc/scx-loader/package.nix |  6 +++++-
 3 files changed, 26 insertions(+), 1 deletion(-)
 create mode 100644 nixos/tests/scx/loader.nix

diff --git a/nixos/tests/all-tests.nix b/nixos/tests/all-tests.nix
index f9ecf72845ede..c75356780dce8 100644
--- a/nixos/tests/all-tests.nix
+++ b/nixos/tests/all-tests.nix
@@ -1490,6 +1490,7 @@ in
   scion-freestanding-deployment = runTest ./scion/freestanding-deployment;
   scrutiny = runTest ./scrutiny.nix;
   scx = runTest ./scx/default.nix;
+  scx-loader = runTest ./scx/loader.nix;
   sddm = import ./sddm.nix { inherit runTest; };
   sdl3 = runTest ./sdl3.nix;
   searx = runTest ./searx.nix;
diff --git a/nixos/tests/scx/loader.nix b/nixos/tests/scx/loader.nix
new file mode 100644
index 0000000000000..788d017f0633b
--- /dev/null
+++ b/nixos/tests/scx/loader.nix
@@ -0,0 +1,20 @@
+{ pkgs, lib, ... }:
+
+{
+  name = "scx-loader";
+  meta.maintainers = with lib.maintainers; [ ccicnce113424 ];
+
+  nodes.machine = {
+    boot.kernelPackages = pkgs.linuxPackages_latest;
+    services.scx-loader = {
+      enable = true;
+      defaultSched = "scx_bpfland";
+    };
+  };
+
+  testScript = ''
+    machine.wait_for_unit("multi-user.target")
+    output = machine.succeed("scxctl get").strip()
+    assert output == "running Bpfland in Auto mode", f"Unexpected scxctl output: {output!r}"
+  '';
+}
diff --git a/pkgs/by-name/sc/scx-loader/package.nix b/pkgs/by-name/sc/scx-loader/package.nix
index 10d76a2765df6..e0f91af71f1ab 100644
--- a/pkgs/by-name/sc/scx-loader/package.nix
+++ b/pkgs/by-name/sc/scx-loader/package.nix
@@ -3,6 +3,7 @@
   rustPlatform,
   lib,
   nix-update-script,
+  nixosTests,
 }:
 rustPlatform.buildRustPackage (finalAttrs: {
   pname = "scx-loader";
@@ -36,7 +37,10 @@ rustPlatform.buildRustPackage (finalAttrs: {
       --replace-fail "/usr/bin/scx_loader" "$out/bin/scx_loader"
   '';
 
-  passthru.updateScript = nix-update-script { };
+  passthru = {
+    updateScript = nix-update-script { };
+    tests = { inherit (nixosTests) scx-loader; };
+  };
 
   meta = {
     mainProgram = "scxctl";
