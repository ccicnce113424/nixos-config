pull_request_rules:
  - name: Merge when labeled and all CI jobs pass
    conditions:
      - and:
          - label=dependencies
          - check-success=All Eval
          - check-success=All Builds
    actions:
      update:
      merge:
        method: squash
