queue_rules:
  - name: default
    merge_method: squash
pull_request_rules:
  - name: Merge when labeled and all CI jobs pass
    conditions:
      - "label=dependencies"
      - "check-success=Check"
      - "check-success=Build LiveCD"
    actions:
      update:
      merge:
        method: squash
